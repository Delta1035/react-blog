on:
  push:
    branches:
      - main

jobs:
  install_dependency_job:
    name: build blog 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 7.14.0
      - uses: actions/setup-node@v3
        with:
          node-version: 14.17.6
          cache:
            "pnpm"
            # cache: "npm"
      - name: "show nodejs version"
        run: node -v
      - name: install dependency
        run: pnpm install --no-frozen-lockfile
        # run: npm i
      - name: run build
        run: npm run build
      - name: Archive production artifacts
        uses: action/upload-artifact@v3
        with: 
          name: dist
          path: dist
      # - name: show secret variable
      #   run: |
      #     echo ${{secrets.FTP_HOST}}
      #     echo ${{secrets.FTP_USER}}
      #     echo ${{secrets.FTP_PASSWORD}}
      #     echo ${{secrets.ftp.FTP_PASSWORD}}
      #     echo ${{secrets.HOST}}
      #     echo ${{secrets.USER}}
      #     echo ${{secrets.PASSWORD}}
      # - name: ftp-action
      #   uses: sebastianpopp/ftp-action@v2.0.0
      #   with:
      #     host: ${{secrets.HOST}}
      #     user: ${{secrets.USER}}
      #     password: ${{secrets.PASSWORD}}
      #     localDir: "dist"
      #     remoteDir: "react-blog"
  build-docker-image:
    name: build docker image
    # 获取install_dependency_job 缓存的工件
    need: install_dependency_job
    runs-on: ubuntu-latest
    steps:
      - name: download dist
        uses: actions/download-artifact@v3
        with:
          name: dist
          path: ./dist
      # docker相关操作
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: 528491526/blog:latest
      # 登录部署服务器 拉取镜像并运行
      # - name: executing remote ssh commands using password
      #   uses: appleboy/ssh-action@v0.1.6
      #   with:
      #     host: ${{ secrets.HOST }}
      #     username: ${{ secrets.SSH_USERNAME }}
      #     password: ${{ secrets.SSH_PASSWORD }}
      #     port: ${{ secrets.SSH_PORT }}
      #     script: |
      #       docker rm -f blog:latest
      #       docker rmi -f blog:latest
      #       docker pull 528491526/blog
      #       docker run --name blog -p 8084:8080 -d blog
      - name: executing remote ssh commands using password
        uses: garygrossgarten/github-action-ssh@release
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          passphrase: ${{ secrets.SSH_PASSWORD }}
          port : ${{ secrets.SSH_PORT}}
          command: |
            docker pull 528491526/blog
            docker run --name blog -p 8084:80 -d 528491526/blog
